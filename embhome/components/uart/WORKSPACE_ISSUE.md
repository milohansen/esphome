# Workspace Issue: Undefined Feature

This component references the feature `esphome-core/esp32c61`, but this feature is not defined in `embhome/core/Cargo.toml`.
This causes build failures across the entire workspace.
