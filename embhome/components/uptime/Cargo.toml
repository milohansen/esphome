[package]
name = "esphome-uptime"
version = "0.1.0"
edition = "2024"

[dependencies]
esphome-core = { path = "../../core" }
esphome-sensor = { path = "../sensor" }
embassy-time = "0.3.2"
embassy-executor = "0.6.0"
async-trait = "0.1"
log = "0.4"
